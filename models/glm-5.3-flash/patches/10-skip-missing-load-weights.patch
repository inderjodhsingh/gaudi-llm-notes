From 0103421f4ab1fc494aadf9ac2042b0a74e979482 Mon Sep 17 00:00:00 2001
From: Gaudi port <gaudi-port@users.noreply.github.com>
Date: Sun, 13 Sep 2026 05:21:01 +0000
Subject: [PATCH] skip missing checkpoint names in glm5next load_weights (tiny
 fixture dense qkv)

---
 vllm/models/glm5next/nvidia/model.py | 4 ++++
 1 file changed, 4 insertions(+)

diff --git a/vllm/models/glm5next/nvidia/model.py b/vllm/models/glm5next/nvidia/model.py
index d1b7eae13..039565d80 100644
--- a/vllm/models/glm5next/nvidia/model.py
+++ b/vllm/models/glm5next/nvidia/model.py
@@ -843,6 +843,8 @@ class Glm5NextModel(nn.Module):
                     name = name.replace(weight_name, param_name)
                     if is_pp_missing_parameter(name, self):
                         continue
+                    if name not in params_dict:
+                        continue
                     param = params_dict[name]
                     weight_loader = param.weight_loader
                     weight_loader(
@@ -868,6 +870,8 @@ class Glm5NextModel(nn.Module):
                     name = remapped_name
                     if is_pp_missing_parameter(name, self):
                         continue
+                    if name not in params_dict:
+                        continue
 
                     param = params_dict[name]
                     weight_loader = getattr(
-- 
2.43.0

